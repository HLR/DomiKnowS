version: v1.0
name: Relational Graph
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804
blocks:
  - name: "Unit tests"
    task:
      prologue:
        commands:
          - checkout
          - export PYTHONPATH=.:$PYTHONPATH
      jobs:
      - name: pytest
        commands:
          - pytest
