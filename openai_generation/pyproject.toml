[project]
name = "domiknows-openai-generation-task"
version = "0.1.0"
description = "OpenAI-compatible generate-then-verify example for DomiKnowS."
requires-python = ">=3.12"
dependencies = [
    "domiknows[cpu]",
    "openai>=1.0.0",
]

[tool.uv]
package = false

[tool.uv.sources]
domiknows = { path = "../..", editable = true }
