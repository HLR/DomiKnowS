[project]
name = "domiknows-real-hmm-comparison-viz-task"
version = "0.1.0"
description = "JSON-driven comparison of DFA, DiscreteHMM, and DomiKnowS-aware HMM internals."
requires-python = ">=3.12"
dependencies = [
    "domiknows[cpu]",
    "torch>=2.0.0",
]

[tool.uv]
package = false

[tool.uv.sources]
domiknows = { path = "../..", editable = true }
